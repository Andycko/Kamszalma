{% extends "_layout" %}

{% block content %}

    {% if entry.contentBuilder.count() %}
        {% for block in entry.contentBuilder.all() %}
            {% include '_content-builder/_blocks/' ~ block.type.handle %}
        {% endfor %}
    {% endif %}

{% endblock %}